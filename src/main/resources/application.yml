spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/user?serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: jwjw0185**

  jpa:
    hibernate:
      #옵션 create, update, none, create-drop, validate 있음
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org:hibernate:SQL: DEBUG